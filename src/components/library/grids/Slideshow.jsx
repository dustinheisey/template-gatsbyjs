import React from 'react'
import styled from 'styled-components'

const StyledContainer = styled.section``

const Gallery1 = ({ children }) => (
  <StyledContainer>{children}</StyledContainer>
)

export default Gallery1
