import { React, Default } from '../../config/imports'

const Content = {
  title: '',
  description: ''
}

export default () => (
  <Default
    title={Content.title}
    description={Content.description}
  />
)
